# Copy to .env and fill in. Never commit .env, and never include it in a zip.

# ── Server ────────────────────────────────────────────────────────────
PORT=3000

# Loopback by default. Only change this if the port is firewalled to your own
# IP, or if you are running behind a reverse proxy that terminates TLS.
BIND_HOST=127.0.0.1

# Shared secret every caller must send as `X-Auth-Token`. Required, min 32 chars.
#   openssl rand -hex 32
AUTH_TOKEN=

# Exact browser origins allowed to call this API. Comma-separated. No wildcards.
# Leave empty if only your scanner (server-to-server) calls it.
ALLOWED_ORIGINS=https://your-site.netlify.app

RATE_LIMIT_PER_MINUTE=30

# Number of reverse proxies in front of this process, for reading the real
# client IP out of X-Forwarded-For. 0 when nothing is in front (the default on
# a VM); 1 behind Caddy or nginx. Set it to 1 with the port directly reachable
# and the rate limiter can be walked past with a forged header.
TRUST_PROXY=0

# ── Safety ────────────────────────────────────────────────────────────
# Must be exactly true or false. A typo is a startup failure, not a silent
# fallback to live trading.
USE_TESTNET=true

# true = compute and log the order, send nothing. Leave true until the plans in
# the log look right. This is the arming switch.
DRY_RUN=true

# ── Sizing and risk ───────────────────────────────────────────────────
# Percent of free margin balance used as NOTIONAL per trade. At 5x leverage a
# value of 5 consumes roughly 1% of the balance as margin.
TRADE_BALANCE_PERCENTAGE=5

# Enforced at order time so sizing is deterministic rather than inheriting
# whatever was last set in the exchange UI.
LEVERAGE=3

# Hard cap on total exposure per symbol, in quote currency. Strongly recommended.
MAX_POSITION_NOTIONAL_QUOTE=500

# The same ceiling as a percentage of account value. A fixed figure REFUSES
# rather than clamps, so the day your balance grows past it every trade starts
# failing for a number you set months earlier. This one scales and never needs
# revisiting. Both may be set; the tighter applies, which makes the fixed one
# useful as an absolute disaster ceiling.
MAX_POSITION_PERCENT=10

# Every market has its own floor — a lot step and a minimum order value — and
# on a small balance those land at very different percentages per symbol (UNI
# might need 63%, SOL 128%, BTC 1004% of the same account). With this on, an
# order below the floor is RAISED to it instead of refused, so one
# TRADE_BALANCE_PERCENTAGE works across a whole watchlist.
#
# It only ever increases size. MAX_POSITION_NOTIONAL_QUOTE and the liquidation
# check still bound the result, so a market whose floor is too large for the
# account is still refused — just for the right reason.
#
# Note the risk becomes uneven: the floor on an expensive symbol can be many
# times the floor on a cheap one. Lower MAX_POSITION_NOTIONAL_QUOTE if you want
# the expensive ones excluded rather than sized up.
MIN_NOTIONAL_BUMP=false

# Protective stop attached to every entry, as a percent from fill price.
STOP_LOSS_PERCENT=2

# Repeat signals with the same exchange/symbol/side inside this window are
# ignored, so a retrying scanner cannot double your position.
DEDUPE_TTL_MS=60000

# Where the circuit breaker stores its daily baseline. It MUST survive a
# restart: without it, a crash-restart re-baselines at the reduced equity and
# the daily loss limit applies all over again to what is left.
# Leave unset on a VM (defaults to this directory). On Render, mount a
# persistent disk and set this to its mount path, e.g. /var/data
# STATE_DIR=

ORDER_TIMEOUT_MS=20000

# ── Exchange credentials ──────────────────────────────────────────────
# Create keys with trade permission only. No withdrawal permission.
# Restrict them to your server's IP in the exchange dashboard.
BYBIT_API_KEY=
BYBIT_API_SECRET=

# Note: ccxt cannot put Weex into sandbox mode. With USE_TESTNET=true the
# server refuses to start while Weex credentials are present, rather than
# trading live funds under a testnet flag.
WEEX_API_KEY=
WEEX_API_SECRET=
# The passphrase chosen when the key was created. ccxt calls it "password" and
# marks it mandatory for weex, so a key/secret pair alone will not authenticate.
WEEX_API_PASSWORD=

# Smallest order value worth placing, in quote currency. ccxt reports no
# limits.cost.min for some Bybit markets, so without this a percentage of a
# small balance can round down to loose change — a real $0.25 order went
# through on RAVE that way, and Bybit ACCEPTED it. So this is an economic
# floor, not a validity one. The exchange's own minimum still wins when it is
# higher. Set 0 to defer entirely to whatever the exchange will accept.
MIN_ORDER_NOTIONAL_QUOTE=1

# Which exchanges may fall back to CURRENT equity when the day's baseline
# cannot be rebuilt from their ledger after a restart.
#
# A list of exchange ids, not a switch: ccxt reads Bybit's ledger cleanly,
# while Weex reports every entry as an inflow and cannot be totalled at all.
# A global on/off would relax Bybit for failures it has never had, just to
# unblock Weex.
#
#   (empty)  refuse to open positions until a baseline exists. Default.
#   weex     that venue baselines from current equity instead, loudly. Its
#            daily limit then measures from process start, not UTC midnight,
#            so a restart mid-way through a losing day forgives what came
#            before. Bybit keeps the strict behaviour.
#   all      every exchange. Rarely what you want.
BREAKER_FALLBACK_TO_EQUITY=
